#include <iostream>
using namespace std;

int main(){

    // Bitwise & (AND)
    int a = 6, b = 10;
    cout << (a & b) << endl;

    // Bitwise | (OR)
    cout << (a | b) << endl;

    // Bitwise ^ (XOR)
    cout << (a ^ b) << endl;
    return 0;
}