#include <iostream>
using namespace std;

int main(){

    // Bitwise << (Left Shift Operator)
    cout << (10 << 2) << endl;
    // cout << (10 << 2) << endl;

    // Bitwise >> (Right Shift Operator)
    cout << (10 >> 1) << endl;
    // cout << (10 >> 2) << endl;

    return 0;
}